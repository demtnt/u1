# u1
1st stage home task

#### Implementation explanation
Before `compileJava` gradlew task `openApiGenerate` task is executed which generates model and controller files in `src/main/java-generated` 

#### Run Tests
`gradlew clean test`

#### Run locally
`gradlew bootRun`

#### Check via UI
It is easy tu use SwaggerUI (by SpringFox) for executing REST methods

http://localhost:8765/swagger-ui.html

#### Check by http requests 
`src/test/resources/*.*`

